{
  "name": "ecommerce-dod",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "db:pack": "node scripts/pack_db.js",
    "engine:build": "node components/build.js",
    "predev": "npm run db:pack && npm run engine:build",
    "dev": "vite",
    "prebuild": "npm run db:pack && npm run engine:build",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "top-level-await": "^1.1.0",
    "vite": "^7.3.1",
    "vite-plugin-top-level-await": "^1.6.0",
    "vite-plugin-wasm": "^3.5.0"
  }
}
